import 'package:clash_royale_cards/app/domain/entities/icon_urls_entity.dart';

class RoyaleEntity {
  String? name;
  int? id;
  int? maxLevel;
  IconUrlsEntity? iconUrls;

  RoyaleEntity({this.name, this.id, this.maxLevel, this.iconUrls});
}
